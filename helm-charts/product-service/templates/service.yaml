apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.product_service.name }}
  labels:
    app: {{ .Values.product_service.name }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.product_service.service.type }}
  ports:
  - port: {{ .Values.product_service.service.port }}
    targetPort: {{ .Values.product_service.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.product_service.name }}